ticketid,punter-name,punter-email,order.id,orderer
<% @tickets.each do |t| -%>
  <%= t.id %>,<%= t.punter.name %>,<%= t.punter.email %>,<%= t.order.id %>,<%= t.order.owner.name_with_email %>
<% end -%>
